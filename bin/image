#!/bin/bash

# GCP project name
# PROJECT=$(gcloud config get-value project)

# modules
go mod tidy
go mod vendor

gcloud builds submit \
    --project cloudylabs-public \
    --tag gcr.io/cloudylabs-public/pubsub-to-bigquery-pump:0.0.3s \
    .
